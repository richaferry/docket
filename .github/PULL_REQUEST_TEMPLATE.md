## What does this change do?

<!-- A short description of the change and why it's needed. -->

## How was this tested?

<!-- npm run lint / npx tsc --noEmit / npm run build, plus any manual steps
     through the UI. Screenshots are welcome for UI changes. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] I generated a migration with `npm run db:generate` if I changed `src/db/schema.ts`
